import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

// Get rid of this when we know what we are doing.
module.exports = {
  eslint: {
    ignoreDuringBuilds: true,
  }
}

export default nextConfig;
